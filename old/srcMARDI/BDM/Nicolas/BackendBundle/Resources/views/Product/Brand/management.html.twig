{% extends 'BDMNicolasBackendBundle::layout.html.twig' %}

{% block content %}

    <section id="main-content">
        <section class="wrapper">
            <div class="row">


                {{ render(controller('BDMNicolasBackendBundle:Partials:flashbag')) }}

                <div class="col-lg-12">
                    <section class="panel">

                        <header class="panel-heading">
                            Gestion Des marques produits
                        </header>
                        <div class="panel-body">
                            <section id="unseen">
                                <div class="clearfix">
                                    <div class="btn-group">
                                        <button id="editable-sample_new" class="btn btn-primary" onclick="window.location='{{ url('_backend_product_brand_add') }}'">
                                            Ajouter <i class="fa fa-plus"></i>
                                        </button>
                                    </div>
                                </div>
                                <br />

                                {{ render(controller('BDMNicolasBackendBundle:Partials:flashbag')) }}

                                <br />
                                {% if brands|length !=0 %}
                                    <table class="table table-bordered table-striped table-condensed">
                                        <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>Titre</th>
                                            <th>Order</th>
                                            <th>State</th>
                                            <th>Action</th>
                                        </tr>
                                        </thead>
                                        <tbody>

                                        {% for brand in brands %}
                                            <tr>
                                                <td>{{ brand.id }}</td>
                                                <td>{{ brand.label }}</td>
                                                <td>{{ brand.order }}</td>
                                                <td>
                                                    {% if brand.isActive == 1 %}
                                                        <span class="label label-success label-mini">Actif</span>
                                                    {% else %}
                                                        <span class="label label-warning label-mini">Inactif</span>
                                                    {% endif %}
                                                </td>
                                                <td>
                                                    <button onclick="window.location='{{ url('_backend_product_brand_edit', {'id':brand.id}) }}'" type="button" class="btn btn-info btn-xs"><i class="fa fa-edit"></i> Modifier</button>
                                                    <button onclick="window.location='{{ url('_backend_product_brand_delete', {'id':brand.id }) }}'"
                                                            type="button" class="btn btn-danger btn-xs"><i
                                                                class="fa fa-trash-o"></i> Supprimer
                                                    </button>
                                                    {% if brand.isActive == 1 %}
                                                        <button onclick="window.location='{{ url('_backend_product_brand_state', {'id':brand.id, 'state': 0}) }}'" type="button" class="btn btn-warning btn-xs"><i class="fa fa-minus-square"></i> Désactiver</button>
                                                    {% else %}
                                                        <button onclick="window.location='{{ url('_backend_product_brand_state', {'id':brand.id, 'state': 1}) }}'" type="button" class="btn btn-success btn-xs"><i class="fa fa-plus-square"></i> Activer</button>
                                                    {% endif %}
                                                </td>
                                            </tr>
                                        {% endfor %}

                                        </tbody>
                                    </table>
                                {% endif %}
                            </section>
                        </div>
                    </section>
                </div>
            </div>
        </section>
    </section>
{% endblock %}

